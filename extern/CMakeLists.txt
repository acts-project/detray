add_subdirectory(algebra-plugins)

if (DETRAY_UNIT_TESTS)
  if( TARGET "gtest")
    message("gtest has been already included...")
    return()
  elseif()
    add_subdirectory(googletest)
  endif()
endif()

if (DETRAY_IO_CSV)
  if( TARGET "gtest")
    message("gtest has been already included...")
  endif()
  
  if( TARGET "dfelibs" )
    message("dfelibs has been already included...")
  elseif()
    add_subdirectory(dfelibs)    
  endif() 
endif()

if(DETRAY_DISPLAY)
    add_subdirectory(matplotplusplus)
endif()
